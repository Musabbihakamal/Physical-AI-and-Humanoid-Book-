name: Deploy Backend to Heroku

on:
  push:
    branches: [ main, master ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        cd backend
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Deploy to Heroku
      uses: akhileshns/heroku-deploy@v3.12.12
      with:
        heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
        heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
        heroku_email: ${{ secrets.HEROKU_EMAIL }}
        healthcheck: 'https://your-app-name.herokuapp.com/health'
        rollback-on-healthcheck-failure: true
      env:
        HD_OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        HD_QDRANT_URL: ${{ secrets.QDRANT_URL }}
        HD_QDRANT_API_KEY: ${{ secrets.QDRANT_API_KEY }}
        HD_DATABASE_URL: ${{ secrets.DATABASE_URL }}
        HD_SECRET_KEY: ${{ secrets.SECRET_KEY }}
        HD_POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}